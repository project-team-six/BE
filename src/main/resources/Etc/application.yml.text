
cloud:
  aws:
    credentials:
      access-key:
      secret-key:
    s3:
      bucket:
    region:
      static: ap-northeast-2
    stack:
      auto: false
# multipartFile 용량 늘려주는 설정
spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  profiles:
    include: KEY


data:
   redis:
      host: localhost # 배포시 redis로 변경
      port: 6379

kakao:
  login:
    redirect:
      uri: http://localhost:/auth/kakao/login

